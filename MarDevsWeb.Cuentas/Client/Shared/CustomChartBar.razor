
@using ChartJs.Blazor.BarChart
 
 <div style="display:contents;">         
                 <div class="card card-extras shadow p-3 bg-white rounded"  style="margin-bottom:5px; padding:0px!important;">                                                                                
                     <div class="card-body" style="padding:0px!important">                                                                                            
                         <Chart Config="_pieConfig"></Chart>
                     </div>                      
                 </div>
             </div>        


    


@code {
    private BarConfig _pieConfig;

    [Parameter] public bool ShowTitle { get; set; } = false;
    [Parameter] public string Title { get; set; }
    [Parameter] public List<string> Labels { get; set; } = new List<string>();
    [Parameter] public List<object> Sources { get; set; } = new List<object>();
    [Parameter] public IndexableOption<string> Colors { get; set; }

    protected override void OnInitialized()
    {
        ConfigurePieConfig();
    }
    private void ConfigurePieConfig()
    {

        _pieConfig = new BarConfig();

        _pieConfig.Options = new BarOptions
            {
                Responsive = true,
                Title = new OptionsTitle
                {
                    Display = ShowTitle,
                    Text = Title
                }                
            };

        foreach (var party in Labels)
        {
            _pieConfig.Data.Labels.Add(party);
        };

        var dataSet = new BarDataset<object>(Sources)
            {
                Label= "",
                BackgroundColor = Colors
            };

            _pieConfig.Data.Datasets.Add(dataSet);


    }

}


